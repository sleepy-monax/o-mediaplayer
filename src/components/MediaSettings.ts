interface MediaSettings {
    src: string;
    thumbnail: string;
    width?: number;
    height?: number;
    muted?: boolean;
    loop?: boolean;
    autoplay?: boolean;
    showControls?: boolean;
    allowFullscreen?: boolean;
}
